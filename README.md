# DoorHandler: Мод для создания интерактивных дверей и ключей
*используется [Mine Language](https://github.com/RuslanBelarus/Mine-Language)*

**Mine Language — Door & Key Module** — это мощное расширение для языка программирования внутри Minecraft, которое добавляет в игру систему интерактивных объектов: дверей, которые можно открывать, и ключей к ним. Мод использует внутренние механизмы игры (сущности *interaction*, *item_frame*) для создания динамичных и настраиваемых головоломок, квестов и RPG-механик без необходимости писать сложные команды вручную.

## 🚀 Возможности
- **Создание дверей:** Объявляйте уникальные объекты-двери с помощью простого синтаксиса.

- **Создание ключей:** Привязывайте ключи к конкретным дверям. Ключи отображаются в мире как невидимые рамки (item_frame).

- **Инициализация объектов:** Размещайте двери и ключи в заданных координатах мира.

- **Логика открывания:** Реализуйте проверку на наличие нужного ключа у игрока. Дверь можно открыть, только если в руке у игрока находится правильный ключ.

- **Интерактивные сообщения:** Игрок получает действиебар-сообщение "Нужен ключ", если пытается открыть дверь без нужного предмета.

- **Игровая интеграция:** Мод использует ванильные механики (крафт из командных блоков, теги, NBT), что обеспечивает совместимость и стабильность.

## 🛠️ Как это работает (Технические детали)
Мод действует как препроцессор. Вы пишете код на упрощённом языке, а мод транслирует его в сложные команды Minecraft.

### Объявление типов:

- *create \<door\> 'name'* — Создаёт новый объект типа "дверь" с заданным именем.

- *create \<key\> 'name'* — Создаёт новый объект типа "ключ" с заданным именем.

### Инициализация (размещение в мире):

- *init \<door\> 'door_name'* — Спавнит дверь как сущность interaction (невидимая область для клика) и очищает блок под ней.

- *init \<key\> 'key_name' vc 'x' 'y' 'z'* — Размещает ключ в виде невидимого item_frame с заданными координатами. Ключ наследует модель предмета от родительской двери (в будущем можно расширить).

### Добавление логики:

- *openable <door_name> — Навешивает на дверь скрипт проверки. При взаимодействии:

  - Проверяет, есть ли у игрока в руке предмет с тегом ключа.
    - Если ключ есть — активирует механизм открывания (ставит редстоун-факел) и удаляет 1 ключ из инвентаря.

    - Если ключа нет — показывает сообщение и отменяет взаимодействие.

  - После активации (постановки факела) дверь-сущность удаляется.

## 📁 Требования
- **Mine Language** (базовый фреймворк/мод-загрузчик).

- **Minecraft Java Edition** (версия, совместимая с сущностью *interaction* — относительно свежие версии).

- Базовое понимание координат в Minecraft и работы с командными блоками / дата-паками.

## ⚙️ Установка и использование
1. Убедитесь, что у вас установлен и работает основной мод **Mine Language**.

2. Скопируйте файл с кодом (например, *door_handler.py*) в соответствующую директорию мода (папку с модулями/хендлерами).

3. В своём основном скрипте для Mine Language используйте новый синтаксис для создания механик.

## 📝 Пример кода (на Mine Language)
``` mine-language
pack Studio.mh
pack DoorHandler.mh

create <door> dungeon_gate
create <key> dungeon_key

fn gameinit
init <door> dungeon_gate
init <key> dungeon_key vec3 120 65 -340
end

fn main
openable dungeon_gate
end

fn setup
execute gameinit
end
````
**Этот код сгенерирует необходимые команды Minecraft для создания полноценной механики "найди ключ, чтобы открыть дверь".**
